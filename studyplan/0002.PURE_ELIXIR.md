Fase 1 — Tokenizador
  → baixar o texto
  → tokenização por caractere
  → vocabulário + encode/decode

Fase 2 — Embedding
  → tabela de embeddings (lista de listas)
  → positional encoding

Fase 3 — Atenção
  → produto escalar Query × Key
  → softmax
  → weighted sum dos Values

Fase 4 — Multi-head
  → N cabeças em paralelo
  → concatenação

Fase 5 — Feed-forward
  → duas camadas lineares + ReLU

Fase 6 — Transformer block
  → junta tudo + layer norm + residual

Fase 7 — Treino
  → forward pass
  → cross-entropy loss
  → backprop manual
  → gradient descent

Fase 8 — Geração
  → loop autorregressivo
  → amostragem